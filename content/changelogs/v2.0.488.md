## Performance Improvements

- Health check endpoint no longer queries the database — reads in-memory atomic timestamps instead, eliminating DB contention that caused tray icon blinking

## Improvements

- Unified recording configuration into a single RecordingConfig struct
- Added accessibility content type for search API
- Show UI events (keystrokes, clicks, clipboard) in timeline popover

## Bug Fixes

- Fix timeline popover scroll blocked by timeline wheel handler
- Fix timeline UI events not showing because frame_id was never set
- Accept short API keys for custom providers (e.g. Ollama)
- Fix Apple CA certificate import for codesigning

#### **Full Changelog:** [85d0ca58..25460f22](https://github.com/mediar-ai/screenpipe/compare/85d0ca58..25460f22)
